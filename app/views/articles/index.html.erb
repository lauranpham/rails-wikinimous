<h1>Articles</h1>

<%= link_to "Add +", new_article_path, class: 'btn btn-primary'%>
<br>

<div class="articles">
  <% @articles.each do |article| %>
    <h2 class="title-link"><%= link_to article.title, article_path(article)%></h2>
    <p><%= article.content %></p>
  <% end %>
</div>
